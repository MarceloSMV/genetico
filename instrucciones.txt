Actúa como un Ingeniero de Software y Científico de Datos experto en Python y algoritmos heurísticos. Necesito que desarrolles un proyecto completo para resolver el Problema del Agente Viajero (TSP) utilizando un Algoritmo Genético Generacional. 

El proyecto debe estar preparado para ejecutarse en un entorno Windows bajo la ruta específica "E:\Proyectos\genetico" y utilizando la versión de Python 3.12.3.

A continuación, detallo los requerimientos técnicos y funcionales que debes cumplir estrictamente al generar la respuesta y el código:

1. ESTRUCTURA Y ENTORNO DEL PROYECTO:
- Indica los comandos exactos de consola (CMD/PowerShell) para crear la carpeta en "E:\Proyectos\genetico" (si no existe), navegar a ella, crear un entorno virtual llamado "venv" usando Python 3.12.3 y activarlo.
- Proporciona un archivo 'requirements.txt' con las librerías necesarias (por ejemplo, matplotlib, numpy, geopy, etc.).

2. DATOS Y LÓGICA MATEMÁTICA:
- El problema debe aplicarse al caso real de las distancias entre los departamentos del Perú (o al menos una muestra representativa de 10-15 departamentos con sus coordenadas reales de latitud y longitud).
- CRÍTICO: Para calcular la distancia entre cada punto geográfico, debes implementar y utilizar estrictamente la fórmula matemática de Haversine. No uses distancias euclidianas.

3. ALGORITMO GENÉTICO GENERACIONAL:
- Codificación: Cada cromosoma debe representar una ruta válida (sin repetir ciudades).
- Fitness: La función objetivo debe minimizar la distancia total de la ruta calculada con Haversine.
- Selección: Implementa un método válido (Ruleta o Torneo).
- Cruce y Mutación: Debes usar operadores diseñados específicamente para el TSP (por ejemplo, Order Crossover - OX1) para evitar generar rutas inválidas con ciudades duplicadas.
- Reemplazo: El enfoque debe ser generacional (los hijos reemplazan a los padres en cada iteración).

4. CRITERIO DE PARADA Y OPTIMIZACIÓN:
- El algoritmo no debe ejecutarse infinitamente ni por un número fijo y estático de generaciones si ya no hay progreso. 
- Implementa un mecanismo de "Early Stopping": el entrenamiento debe detenerse automáticamente cuando la mejor distancia (fitness) no haya mostrado ninguna mejoría tras un número determinado de generaciones (por ejemplo, 50 generaciones sin cambios). Esto evitará el "trabajo innecesario".

5. VISUALIZACIÓN:
- Al finalizar (o durante) la ejecución, el script debe generar un gráfico bidimensional (usando matplotlib) que muestre la "Distancia" en el eje Y versus las "Generaciones" en el eje X. Esto permitirá visualizar la convergencia del algoritmo.

6. DOCUMENTACIÓN (RUN.md):
- Como parte de tu respuesta, debes generar el contenido de un archivo llamado 'RUN.md'.
- Este archivo debe contener las instrucciones claras, paso a paso, para la puesta en marcha del proyecto desde cero para un usuario que acaba de clonar o descargar el código. Debe incluir la activación del entorno virtual y la ejecución del script principal.

Por favor, entrégame el código de Python estructurado (preferiblemente en un archivo principal como 'main.py' o modularizado si lo consideras mejor), el contenido de 'requirements.txt' y el contenido de 'RUN.md'. Asegúrate de que el código esté fuertemente comentado explicando qué hace cada paso del algoritmo genético.